import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-test-routing',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
